export const short = {
  en_US: 'Self-hosted sync server for Standard Notes',
}

export const long = {
  en_US:
    'Standard Notes Server is the self-hosted backend for the Standard Notes end-to-end encrypted notes apps. This package runs the all-in-one home server: a single Node process bundling sync, auth, files, and revisions, backed by an embedded SQLite database and on-disk file storage. No external MySQL, Redis, or object storage is required. Point your Standard Notes desktop, mobile, or web app at this server to keep your encrypted notes, tags, and revision history syncing across devices on hardware you control. All data lives in the service volume and is captured by StartOS backups.',
}
